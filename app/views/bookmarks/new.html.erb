<%= simple_form_for [@list, @bookmark] do |f| %>
  <%= f.input :comment %>
  <%= f.association :movie, collection: Movie.all %>
  <%= f.button :submit, class: 'btn btn-success mt-3' %>
<% end %>
<%= link_to 'Back to list', list_path(@list), class: 'btn btn-primary mt-3' %>
